# Server
server.port=${PORT:8080}

# H2 console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA / H2
spring.datasource.url=jdbc:h2:mem:burgerdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

# ⚠️ IMPORTANTE - Cargar data.sql DESPUÉS de crear tablas
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# CORS allowed origin
app.frontend.origin=https://federicovaldez14.github.io/Burger-Planet
